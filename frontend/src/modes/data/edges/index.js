/**
 * Data Mode — Custom Edge Types
 *
 * Each file exports a single React Flow custom edge component.
 * Register all edge types in DataModeApp.jsx via the `edgeTypes` map.
 *
 * Planned for Phase 4+:
 *   DataFlowEdge     — animated edge showing data flowing between nodes
 *   ConditionalEdge  — edge with a condition/filter label
 */
