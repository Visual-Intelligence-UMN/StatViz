/**
 * Data Mode — Components
 *
 * Re-usable UI components for the Data Mode canvas.
 * Examples for Phase 4+:
 *   DataToolbar      — top action bar (upload dataset, add hypothesis, etc.)
 *   DataSidebar      — node inspector / settings panel
 *   UploadOverlay    — drag-and-drop file upload sheet
 *   StatsBadge       — inline statistic display chip
 */
