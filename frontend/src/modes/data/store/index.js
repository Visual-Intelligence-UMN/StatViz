/**
 * Data Mode — State Store
 *
 * Manages all Data Mode application state (nodes, edges, datasets,
 * analysis results). Isolated from Q&A Mode state.
 *
 * Planned for Phase 4+:
 *   useDataModeStore  — Zustand store (or React context) for:
 *     - nodes / edges array
 *     - loaded datasets
 *     - active hypothesis
 *     - analysis run history
 *     - sidebar selection state
 */
