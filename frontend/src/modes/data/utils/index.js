/**
 * Data Mode — Utilities
 *
 * Pure helper functions with no side effects.
 * Safe to unit-test in isolation.
 *
 * Planned for Phase 4+:
 *   parseCsv(file)               → rows: object[]
 *   inferColumnTypes(rows)       → { colName: 'numeric' | 'categorical' | 'date' }[]
 *   formatStatResult(result)     → human-readable string
 *   buildNodePosition(index, of) → { x, y } grid layout helper
 */
