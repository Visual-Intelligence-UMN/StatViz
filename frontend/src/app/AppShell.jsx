import DataModeApp from '../modes/data/DataModeApp';
import './AppShell.css';

function AppShell() {
    return (
        <div className="app-shell">
            <DataModeApp />
        </div>
    );
}

export default AppShell;
